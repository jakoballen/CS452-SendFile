Navigate to the folder containing sendfile.cpp. To compile, run "make" To
execute the file as the server, run "./run server". To execute the file as
a client, run "./run client". The server must be running first and waiting
for the client to connect before the client is started. The server will waiting
for the client after it gathers all of the necessary information.
